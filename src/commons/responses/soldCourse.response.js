class SoldCourse {
  constructor({ id, user, course, createdAt, updatedAt }) {
    this.id = id;
    this.user = user;
    this.course = course;
    this.createdAt = createdAt;
    this.updatedAt = updatedAt;
  }
}

export default SoldCourse;
